$terminal = uwsm app --  kitty
$fileManager = uwsm app --  kitty --class floating -e yazi
#$menu = pgrep -x wofi >/dev/null 2>&1 && killall wofi || uwsm app -- $(wofi --dmenu --show drun --define=drun-print_desktop_file=true)
$menu = pgrep -x fuzzel >/dev/null 2>&1 && killall fuzzel || fuzzel
$screenshot = uwsm app -- hyprshot -m region --clipboard-only

$wallpaper= ~/.cache/wallpaper/current_wallpaper

$taskManager = pgrep -x btop >/dev/null 2>&1 && killall btop || uwsm app -- kitty --class floating -e btop

$superMenu = ~/scripts/menus/menu-menu.sh
